npm version patch --no-git-tag-version && node -e "const a=require('./app.json');a.expo.version=require('./package.json').version;require('fs').writeFileSync('app.json',JSON.stringify(a,null,2))" && git add package.json app.json && git commit -m "chore: v$(node -p "require('./package.json').version")"